# CPU controllers and GPU node agents use the pool labels from node-profiles.yaml.
apiVersion: management.loft.sh/v1
kind: ArgoCDApplicationTemplate
metadata:
  name: demo-gpu-operator
spec:
  displayName: Demo - NVIDIA GPU Operator
  owner:
    team: loft-admins
  parameters:
    - variable: cpuNodePool
      label: CPU Service Pool Label
      description: Value of workload.example.com/pool on CPU service workers.
      type: string
      required: true
      defaultValue: cpu-services
    - variable: version
      label: Chart Version
      type: string
      required: true
      defaultValue: v26.3.3
    - variable: project
      label: Argo CD Project
      type: string
      required: true
      defaultValue: default
    - variable: driverPreinstalled
      label: NVIDIA Driver Preinstalled
      type: boolean
      defaultValue: 'false'
  template:
    spec:
      destination:
        namespace: gpu-operator
      project: '{{ .Values.project }}'
      source:
        chart: gpu-operator
        repoURL: https://helm.ngc.nvidia.com/nvidia
        targetRevision: '{{ .Values.version }}'
        helm:
          releaseName: gpu-operator
          values: |
            operator:
              defaultRuntime: containerd
              nodeSelector:
                kubernetes.io/os: linux
                workload.example.com/pool: {{ .Values.cpuNodePool | quote }}
              tolerations: []
            driver:
              enabled: {{ if eq (.Values.driverPreinstalled | toString) "true" }}false{{ else }}true{{ end }}
              nvidiaDriverCRD:
                enabled: false
                deployDefaultCR: true
                driverType: gpu
              repository: nvcr.io/nvidia
              image: driver
            toolkit:
              enabled: true
            devicePlugin:
              enabled: true
            cdi:
              enabled: true
            mig:
              strategy: single
            migManager:
              enabled: false
            dcgm:
              enabled: true
            dcgmExporter:
              enabled: true
              repository: nvcr.io/nvidia/k8s
              image: dcgm-exporter
              version: 4.5.3-4.8.2-distroless
              enablePodLabels: false
              enablePodUID: false
              config:
                create: true
                name: custom-dcgm-exporter-metrics
                data: |-
                  # Format: DCGM field, Prometheus metric type, help message. Lines starting with '#' are comments.
                  # Supplying config.data replaces the exporter default set, so every metric the shipped dashboards query must be listed here.
                  DCGM_FI_DEV_SM_CLOCK,  gauge, SM clock frequency (in MHz).
                  DCGM_FI_DEV_MEM_CLOCK, gauge, Memory clock frequency (in MHz).
                  DCGM_FI_DEV_MEMORY_TEMP, gauge, Memory temperature (in C).
                  DCGM_FI_DEV_GPU_TEMP,    gauge, GPU temperature (in C).
                  DCGM_FI_DEV_POWER_USAGE,              gauge, Power draw (in W).
                  DCGM_FI_DEV_TOTAL_ENERGY_CONSUMPTION, counter, Total energy consumption since boot (in mJ).
                  DCGM_FI_DEV_PCIE_REPLAY_COUNTER, counter, Total number of PCIe retries.
                  DCGM_FI_DEV_GPU_UTIL,      gauge, GPU utilization (in %).
                  DCGM_FI_DEV_MEM_COPY_UTIL, gauge, Memory utilization (in %).
                  DCGM_FI_DEV_ENC_UTIL,      gauge, Encoder utilization (in %).
                  DCGM_FI_DEV_DEC_UTIL,      gauge, Decoder utilization (in %).
                  DCGM_FI_DEV_XID_ERRORS,    gauge, Value of the last XID error encountered.
                  DCGM_FI_DEV_FB_FREE, gauge, Framebuffer memory free (in MiB).
                  DCGM_FI_DEV_FB_USED, gauge, Framebuffer memory used (in MiB).
                  DCGM_FI_DEV_FB_RESERVED, gauge, Framebuffer memory reserved (in MiB).
                  DCGM_FI_DEV_NVLINK_BANDWIDTH_TOTAL, gauge, Total number of NVLink bandwidth counters for all lanes.
                  DCGM_FI_DEV_VGPU_LICENSE_STATUS, gauge, vGPU License status.
                  DCGM_FI_DEV_UNCORRECTABLE_REMAPPED_ROWS, counter, Number of remapped rows for uncorrectable errors.
                  DCGM_FI_DEV_CORRECTABLE_REMAPPED_ROWS,   counter, Number of remapped rows for correctable errors.
                  DCGM_FI_DEV_ROW_REMAP_FAILURE,           gauge,   Whether remapping of rows has failed.
                  DCGM_FI_DRIVER_VERSION, label, Driver Version.
                  DCGM_FI_PROF_GR_ENGINE_ACTIVE,   gauge, Ratio of time the graphics engine is active.
                  DCGM_FI_PROF_PIPE_TENSOR_ACTIVE, gauge, Ratio of cycles the tensor (HMMA) pipe is active.
                  DCGM_FI_PROF_DRAM_ACTIVE,        gauge, Ratio of cycles the device memory interface is active.
                  DCGM_FI_PROF_PCIE_TX_BYTES,      gauge, Rate of data transmitted over the PCIe bus in bytes per second.
                  DCGM_FI_PROF_PCIE_RX_BYTES,      gauge, Rate of data received over the PCIe bus in bytes per second.
                  # Added by vCluster Platform so the Fleet Observability dashboards populate. Disabled in the upstream default set.
                  DCGM_EXP_XID_ERRORS_COUNT, counter, Count of XID errors within the last counter window.
                  DCGM_FI_DEV_NVLINK_CRC_FLIT_ERROR_COUNT_TOTAL, counter, Total number of NVLink FLIT CRC errors.
                  DCGM_FI_DEV_NVLINK_CRC_DATA_ERROR_COUNT_TOTAL, counter, Total number of NVLink data CRC errors.
              serviceMonitor:
                enabled: false
                interval: 15s
            nodeStatusExporter:
              enabled: true
            nfd:
              enabled: true
            gfd:
              enabled: true
            node-feature-discovery:
              postDeleteCleanup: false
              master:
                nodeSelector:
                  kubernetes.io/os: linux
                  workload.example.com/pool: {{ .Values.cpuNodePool | quote }}
              gc:
                nodeSelector:
                  kubernetes.io/os: linux
                  workload.example.com/pool: {{ .Values.cpuNodePool | quote }}
              worker:
                nodeSelector:
                  kubernetes.io/os: linux
                  workload.example.com/pool: gpu-compute
                tolerations:
                - key: nvidia.com/gpu
                  operator: Exists
                  effect: NoSchedule
            daemonsets:
              tolerations:
              - key: nvidia.com/gpu
                operator: Exists
                effect: NoSchedule
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          backoff:
            duration: 30s
            factor: 2
            maxDuration: 5m
          limit: 10
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
